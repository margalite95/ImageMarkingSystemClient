import { RemoveShareDocumentResponseOK } from './remove-share-document-response-ok';

describe('RemoveShareDocumentResponseOK', () => {
  it('should create an instance', () => {
    expect(new RemoveShareDocumentResponseOK()).toBeTruthy();
  });
});
