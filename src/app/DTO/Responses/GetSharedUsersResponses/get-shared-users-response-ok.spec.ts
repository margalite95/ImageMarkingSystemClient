import { GetSharedUsersResponseOK } from './get-shared-users-response-ok';

describe('GetSharedUsersResponseOK', () => {
  it('should create an instance', () => {
    expect(new GetSharedUsersResponseOK()).toBeTruthy();
  });
});
