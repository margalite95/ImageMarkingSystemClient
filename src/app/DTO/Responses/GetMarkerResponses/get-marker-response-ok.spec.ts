import { GetMarkerResponseOK } from './get-marker-response-ok';

describe('GetMarkerResponseOK', () => {
  it('should create an instance', () => {
    expect(new GetMarkerResponseOK()).toBeTruthy();
  });
});
