import { EditColorsRequest } from './edit-colors-request';

describe('EditColorsRequest', () => {
  it('should create an instance', () => {
    expect(new EditColorsRequest()).toBeTruthy();
  });
});
