import { UnSubscribeUserRequest } from './un-subscribe-user-request';

describe('UnSubscribeUserRequest', () => {
  it('should create an instance', () => {
    expect(new UnSubscribeUserRequest()).toBeTruthy();
  });
});
